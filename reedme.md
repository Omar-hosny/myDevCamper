# myDevcamper API

> Backend api for myDevCamper app
> which is boot camp directory

# usage

rename "config/config.env.env" to
"config/config.env" and update values

# install Depedencies

```
npm install
```

# Run app

```
# run app in dev mood
npm run dev

# run app in prod mood

npm start
```
